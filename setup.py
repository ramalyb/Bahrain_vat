from setuptools import setup, find_packages

setup(
    name="bahrain_vat",
    version="0.0.1",
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
