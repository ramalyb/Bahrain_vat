[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "bahrain_vat"
authors = [{ name = "ERPGulf" }]
dynamic = ["version", "description"]
requires-python = ">=3.10"
dependencies = []

[tool.frappe]
frappe_version = ">=15.0.0"
